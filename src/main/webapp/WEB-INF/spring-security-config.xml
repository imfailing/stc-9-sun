<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config= "true" use-expressions= "true"  >
        <intercept-url pattern="/index*" access="IS_AUTHENTICATED_ANONYMOUSLY" />
        <intercept-url pattern="/signup*" access="IS_AUTHENTICATED_ANONYMOUSLY" />
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
        <form-login login-page="/login.jsp" username-parameter="username" password-parameter="password"  login-processing-url= "/security_check"  authentication-failure-url="/login.jsp?error=1"/>
        <logout logout-success-url="/logout" />
        <csrf />
    </http>

    <authentication-manager>
        <authentication-provider ref='customAuthenticationProvider'/>
    </authentication-manager>

</beans:beans>